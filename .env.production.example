
# Public API endpoint (FastAPI on your VPS)
VITE_API_BASE=https://api.app.shelvin-joel.dev/api

# Base path for the built site.
# Use "/" for a custom domain (app.shelvin-joel.dev) or GitHub Pages with a CNAME.
# Use "/<repo-name>/" if hosting under a repo without a CNAME.
VITE_BASE=/
