import ClientHome from '@/views/ClientHome';

// eslint-disable-next-line react-refresh/only-export-components
export const metadata = {
  title: 'Originals - Moviebay',
};

export default function OriginalsPage() {
  return null;
}
