import Profile from '@/views/Profile';

// eslint-disable-next-line react-refresh/only-export-components
export const metadata = {
  title: 'Moviebay - My Profile',
};

export default function ProfilePage() {
  return <Profile />;
}
