import Admin from '@/views/Admin';

// eslint-disable-next-line react-refresh/only-export-components
export const metadata = {
  title: 'Moviebay - Admin Panel',
};

export default function AdminPage() {
  return <Admin />;
}
